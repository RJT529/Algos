bool compareInt(pair<int,int>p1, pair<int,int>p2)
{
  if(p1.second > p2.second) return true;
  if(p1.second == p2.second) return p1.first < p2.first;
  return false;
}

vector<pair<int,int>> list[n+1];
sort(list[i].begin(),list[i].end(),compareInt);
